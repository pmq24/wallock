$schema: https://moonrepo.dev/schemas/workspace.json

type: application
language: go

project:
  name: API
  description: The middleman server between Web and Goole Cloud Platform

tasks:
  dev:
    command: echo "Run ./dev.sh instead" && exit 1

  format:
    command: go fmt .

  lint-fix:
    command: noop
    deps:
    - '~:format'
