$schema: https://moonrepo.dev/schemas/workspace.json

type: application
language: typescript

project:
  name: Web
  description: The user-facing web application

tasks:
  pre-commit:
    command: noop
    deps:
    - '~:lint'
    - '~:typecheck'
