$schema: https://moonrepo.dev/schemas/workspace.json

type: application

project:
  name: Webserver
  description: The webserver

tasks:
  dev:
    command: echo "Run ./dev.sh instead" && exit 1
  
  lint:
    command: caddy fmt --overwrite
