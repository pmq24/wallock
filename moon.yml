$schema: https://moonrepo.dev/schemas/workspace.json

tasks:
  dev:
    command: >
      docker
        compose
          --file=compose.dev.yml
        up
          --watch
          --build
  prod:
    command: >
      docker
        compose
          --file=compose.prod.yml
        up
